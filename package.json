{
  "scripts": {
    "babel": "npx babel --minified --presets=babel-preset-react ./client/*.jsx --out-dir .",
    "babel-watch": "npx babel --minified --presets=babel-preset-react ./client/*.jsx --out-dir . --watch ./client/*.jsx"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-react": "^6.24.1"
  }
}
